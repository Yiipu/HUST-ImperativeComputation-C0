int main() {
    string[] A = alloc_array(string, 4);

    // Initialize the array elements
    A[0] = "banana";
    A[1] = "cherry";
    A[2] = "apple";
    A[3] = "date";

    int lower = 0;
    int upper = 3;

    // Test the sort function
    sort(A, lower, upper);

    // Assertion: Check if the array is sorted
    //@assert is_sorted(A, lower, upper);

    return 0;
}
